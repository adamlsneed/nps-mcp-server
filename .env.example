# NPS MCP Server Configuration
# Copy to .env and fill in your values: cp .env.example .env

# Required: NPS server URL (include port)
NPS_URL=https://your-nps-server:6500

# ─── Auth Strategy 1: Interactive + Static MFA (recommended for lab/dev) ────
NPS_USERNAME=admin
NPS_PASSWORD=your-password
NPS_MFA_CODE=000000

# ─── Auth Strategy 2: Interactive + MFA Prompt (Claude Code only) ───────────
# NPS_USERNAME=admin
# NPS_PASSWORD=your-password
# NPS_MFA_PROMPT=true

# ─── Auth Strategy 3: Pre-supplied Token (SAML, OIDC, Duo Push) ────────────
# NPS_TOKEN=eyJhbGciOiJSUzI1NiIs...
# Tip: Run `npx nps-auth --save` to get a token and save it here

# ─── Auth Strategy 4: API Key (limited — known NPS role-claims bug) ─────────
# NPS_USERNAME=AppName
# NPS_API_KEY=your-api-key

# Optional: Set to "true" to enforce TLS certificate validation
# Default is false (allows self-signed certs)
NPS_TLS_REJECT=false
